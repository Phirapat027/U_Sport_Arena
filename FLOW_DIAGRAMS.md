# 🔄 Promotion System Flow Diagram

## User Booking Flow with Promotion

```
┌─────────────────────────────────────────────────────┐
│        🏃 USER BOOKING JOURNEY                       │
└─────────────────────────────────────────────────────┘

1️⃣ USER SELECTS TIME SLOTS
   ┌──────────────────────────┐
   │  Select 2 Hours (2,000฿)  │
   └───────────┬──────────────┘
               │
               ▼
2️⃣ ENTERS PROMOTION CODE
   ┌──────────────────────────┐
   │  Input: "SUMMER200"      │
   │  ┌────────────────────┐  │
   │  │ [SUMMER200]  [ใช้]  │  │
   │  └────────────────────┘  │
   └───────────┬──────────────┘
               │
               ▼
3️⃣ SYSTEM VALIDATES
   ┌──────────────────────────┐
   │ ✅ Code exists?          │
   │ ✅ Active status?        │
   │ ✅ Not expired?          │
   └───────────┬──────────────┘
               │
        ┌──────┴──────┐
        │             │
       ✅            ❌
        │             │
        ▼             ▼
   VALID        SHOW ERROR
   CODE         MESSAGE
        │             │
        └──────┬──────┘
               │
               ▼
4️⃣ CALCULATE FINAL PRICE
   ┌─────────────────────────────┐
   │  Base Price:      2,000฿    │
   │  - Discount:       -200฿    │
   │  ─────────────────────────  │
   │  Final Price:     1,800฿   │
   │  (Saved color: 🟢 Green)   │
   └───────────┬─────────────────┘
               │
               ▼
5️⃣ CONFIRM BOOKING
   ┌──────────────────────────┐
   │  [ยืนยันการจอง]           │
   │  Booking + promotion_id   │
   │  saved to database        │
   └───────────┬──────────────┘
               │
               ▼
6️⃣ SUCCESS
   ┌──────────────────────────┐
   │  ✅ จองสนามสำเร็จ!         │
   │  Final Amount: 1,800฿    │
   │  Slots cleared            │
   │  Promotion removed        │
   └──────────────────────────┘
```

---

## Admin Promotion Management Flow

```
┌─────────────────────────────────────────────────────┐
│           ADMIN PROMOTION MANAGEMENT                 │
└─────────────────────────────────────────────────────┘

┌──────────────────────┐
│  [+ เพิ่มโปรโมชั่นใหม่]   │
└──────────┬───────────┘
           │
        1️⃣ CREATE
           │
           ▼
┌──────────────────────┐
│     Create Form      │
│                      │
│  [รหัส]    [....]     │
│  [ส่วนลด]  [300]      │
│  [วันที่]  [....]       │
│                      │
│  [บันทึก] [ยกเลิก]      │
└──────────┬────────────┘
           │
           ▼
      💾 บันทึกลง DB
           │
           ▼
      ✅ เพิ่มสำเร็จ!
           │
           ▼
    กลับสู่ Promotion List


┌───────────────────────┐
│  /admin/promotions    │
│  Promotion List Page  │
└──────────┬────────────┘
           │
           ▼
┌───────────────────────────────────────────────┐
│                Existing Promotions            │
│                                               │
│  ┌────────────────┐                           │
│  │ SUMMER200      │                           │
│  │ ✅ ใช้งาน       │──[ปิด]───┐                │
│  └────────────────┘          │                │
│                              │                │
│  ┌────────────────┐          │                │
│  │ WELCOME20      │          │                │
│  │ ✅ ใช้งาน       │──[ปิด]───┤                │
│  └────────────────┘          │                │
│                              ▼                │
│  ┌────────────────┐    2️⃣ TOGGLE STATUS       │
│  │ EXPIRED2024     │           │               │
│  │ ❌ หมดอายุ      │──[เปิด]────┘               │
│  └────────────────┘            │               │
│         │                      ▼               │
│      [ลบ]              💾 อัปเดตสถานะใน DB     │
│         │                      │               │
└─────────┼──────────────────────┼───────────────┘
          │                      ▼
     3️⃣ DELETE            ✅ อัปเดตสำเร็จ
          │
          ▼
     ⚠️ ยืนยันการลบ
          │
          ▼
     🗑️ ลบออกจาก DB
          │
          ▼
     ✅ ลบสำเร็จ

```

---

## Promotion Calculation Logic

```
┌─────────────────────────────────────┐
│  DISCOUNT CALCULATION ENGINE        │
└─────────────────────────────────────┘

Input: Base Price + Promotion
       │
       ▼
    Is Promotion Valid?
    (Status + Date Check)
       │
    ┌──┴──┐
   NO    YES
    │     │
    │     ▼
    │  Check Discount Type
    │     │
    │  ┌──┴──────────┐
    │  │             │
    │  FIXED AMT    PERCENTAGE
    │  │             │
    │  ▼             ▼
    │  discount  discount
    │  = 200฿   = (price × 20%)
    │  │         │
    │  └────┬────┘
    │       │
    │       ▼
    │  Is discount > price?
    │       │
    │    ┌──┴──┐
    │   YES   NO
    │    │     │
    │    ▼     ▼
    │  CAP   NORMAL
    │  TO    CALC
    │  PRICE
    │    │     │
    └────┬─────┘
         │
         ▼
    Final Price = Base - Discount
         │
         ▼
    ✅ Return Final Price
```

---

## Component Architecture

```
┌─────────────────────────────────────────┐
│       COMPONENT HIERARCHY               │
└─────────────────────────────────────────┘

┌─ app/reservation/page.tsx ──────────────┐
│                                         │
│  ReservationDetails                     │
│  ├─ Stadium Info                        │
│  └─ Booking Section                     │
│     ├─ Time Slots (local state)         │
│     ├─ PromotionInput ◄────────┐        │
│     │  ├─ Input field          │        │
│     │  ├─ Apply button         │        │
│     │  └─ Success/Error msg    │        │
│     │                          │        │
│     └─ Price Display           │        │
│        ├─ Base Price           │        │
│        ├─ Discount (if any) ◄──┘        │
│        └─ Final Price                   │
│                                         │
│  Logic:                                 │
│  - selectedSlots (useState)             │
│  - appliedPromotion (useState)          │
│  - handleApplyPromotion()               │
│  - calculateFinalPrice()                │
│  - handleBooking()                      │
│                                         │
└─────────────────────────────────────────┘

┌─ app/admin/promotions/page.tsx ─────────┐
│                                         │
│  PromotionsAdmin                        │
│  ├─ Header + Add button                 │
│  ├─ Form (conditional)                  │
│  │  ├─ Name input                       │
│  │  ├─ Description input                │
│  │  ├─ Discount type select             │
│  │  ├─ Discount value input             │
│  │  ├─ Date pickers                     │
│  │  ├─ Status select                    │
│  │  └─ Buttons                          │
│  │                                      │
│  └─ Promotions Grid                     │
│     └─ PromotionCard (multiple)         │
│        ├─ Name + description            │
│        ├─ Discount display              │
│        ├─ Date range                    │
│        ├─ Valid badge                   │
│        └─ Action buttons                │
│                                         │
│  Logic:                                 │
│  - promotions (useState)                │
│  - formData (useState)                  │
│  - handleAddPromotion()                 │
│  - handleDeletePromotion()              │
│  - handleToggleStatus()                 │
│                                         │
└─────────────────────────────────────────┘
```

---

## Data Flow (User Side)

```
┌────────────────────────────────────────┐
│         DATA FLOW - USER BOOKING       │
└────────────────────────────────────────┘

  📱 UI Input
     │
     ├─► Select Time Slots
     │   └─► setSelectedSlots()
     │       │
     │       ▼
     │   Display: basePrice = slots × 1000
     │
     ├─► Enter Code
     │   └─► searchPromotionByCode(code)
     │       │
     │       ▼
     │   Is Promo Valid?
     │   ├─► Yes: setAppliedPromotion(promo)
     │   │       │
     │   │       ▼
     │   │   Calculate:
     │   │   - discount = calculateDiscount()
     │   │   - finalPrice = calculateFinalPrice()
     │   │       │
     │   │       ▼
     │   │   Display:
     │   │   - basePrice ฿
     │   │   - discount ฿ (green)
     │   │   - finalPrice ฿ (green)
     │   │
     │   └─► No: setError(msg)
     │       Display error message
     │
     └─► Click Confirm
         └─► handleBooking()
             │
             ├─► Get user session
             ├─► Save to DB:
             │   {
             │     field_id,
             │     user_id,
             │     time_slot,
             │     promotion_id ◄─── Key!
             │   }
             │
             ▼
         ✅ Success Alert
         - setSelectedSlots([])
         - setAppliedPromotion(null)
         - Reset form
```

---

## Database Integration (Future)

```
┌─────────────────────────────────────────┐
│  SUPABASE INTEGRATION                   │
└─────────────────────────────────────────┘

Supabase
    │
    ▼
┌─────────────────────────────────────────┐
│ Table: promotions                       │
├─────────────────────────────────────────┤
│ id (UUID)                               │
│ name (TEXT) ◄─── SEARCH KEY             │
│ description (TEXT)                      │
│ discount_percentage (DECIMAL)           │
│ discount_amount (DECIMAL)               │
│ valid_from (TIMESTAMP)                  │
│ valid_until (TIMESTAMP)                 │
│ status (TEXT: active/inactive)          │
│ created_at (TIMESTAMP)                  │
│ updated_at (TIMESTAMP)                  │
└─────────────────────────────────────────┘
    ▲           ▲
    │           │
  READ      WRITE/UPDATE
    │           │
    ├─► searchPromotionByCode()  (Admin)
    │            │
    │            ├─► Create promo
    │            ├─► Update status
    │            └─► Delete promo
    │
    └─► (User booking)
         └─► Save promotion_id to bookings table
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────┐
│  ERROR HANDLING SCENARIOS               │
└─────────────────────────────────────────┘

🔴 Invalid Code
    └─► await searchPromotionByCode()
        └─► Returns null
            └─► setError("รหัสส่วนลดไม่ถูกต้อง")
                └─► Display red message

🔴 Expired Code
    └─► isPromotionValid()
        ├─► Check status === 'active' ❌
        └─► OR Check dates: now > valid_until ❌
            └─► setError("โปรโมชั่นหมดอายุแล้ว")
                └─► Display red message

🟡 Empty Code
    └─► !code.trim()
        └─► setError("กรุณากรอกรหัสส่วนลด")
            └─► Display yellow message

✅ Success
    └─► searchPromotionByCode() ✓
        └─► isPromotionValid() ✓
            └─► setAppliedPromotion(promo)
                └─► setSuccess(true)
                    └─► Display green message
```

---

## Price Calculation Examples

```
┌─────────────────────────────────────────┐
│  PRICE CALCULATION EXAMPLES             │
└─────────────────────────────────────────┘

Example 1: Fixed Discount
━━━━━━━━━━━━━━━━━━━━━━━━
Base Price:      2,000฿
Promo: SUMMER200 (200฿)
─────────────────────────
Discount:        -200฿
Final Price:     1,800฿
Savings:         10% ✅


Example 2: Percentage Discount
━━━━━━━━━━━━━━━━━━━━━━━━━━
Base Price:      1,000฿
Promo: WELCOME20 (20%)
─────────────────────────
Calculation:     1000 × 20% = 200฿
Discount:        -200฿
Final Price:     800฿
Savings:         20% ✅


Example 3: Discount > Price
━━━━━━━━━━━━━━━━━━━━━━━━
Base Price:      100฿
Promo: VIPUSER500 (500฿)
─────────────────────────
Discount (capped): -100฿  ⚠️ CAP
Final Price:     0฿
Savings:         100% (MAX) ✅
```

---

## Status Indicators

```
┌─────────────────────────────────────────┐
│  UI STATUS INDICATORS                   │
└─────────────────────────────────────────┘

✅ PROMOTION ACTIVE & VALID
   └─► Green badge: "ใช้งาน"
       Green card border
       Show in list

⏰ PROMOTION EXPIRED
   └─► Red badge: "หมดอายุ"
       Red border
       Show error: "โปรโมชั่นหมดอายุแล้ว"
       Still show in admin (for records)

❌ PROMOTION INACTIVE
   └─► Gray badge: "ปิด"
       Gray card
       Hidden from users
       Admin can toggle back

🟢 PROMOTION APPLIED (User View)
   └─► Green alert box
       Shows: "✅ ใช้โปรโมชั่นสำเร็จ!"
       Show remove button
       Calculate discount

❌ PROMOTION ERROR
   └─► Red alert box
       Shows: error message
       Examples:
       - "รหัสส่วนลดไม่ถูกต้อง"
       - "โปรโมชั่นหมดอายุแล้ว"
       - "กรุณากรอกรหัสส่วนลด"
```

---

## Timeline Example

```
PROMOTION LIFECYCLE
═══════════════════════════════════════════════

2025-01-01  2025-06-01  2025-12-31  2026-01-01
    │           │           │            │
    │◄──────────┤           │            │
    │    ACTIVE PERIOD      │            │
    │           │           │            │
   START       MIDDLE      END         EXPIRED
                                           │
                                    ❌ Can't use


Admin Actions Timeline:
─────────────────────

JAN-01: ✅ Create promo (valid_from: 2025-01-01, valid_until: 2025-12-31)
         │
JAN-15: ✅ Active & Users can use
         │
JUN-01: ✅ Still active (in middle)
         │
DEC-30: ✅ Last chance (1 day left)
         │
DEC-31: ⏰ EXPIRATION TIME
         │
JAN-01: ❌ Expired (Users cannot use)
         │
JAN-10: ✅ Admin can still view & delete old records
```

---

**Last Updated:** January 2025  
**System:** U-Sport Arena Promotion System
